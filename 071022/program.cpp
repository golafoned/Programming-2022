#include <iostream>

using namespace std;
int main()
{
     setlocale(LC_ALL, "Ukr");

     // string str = "If you are connected but behind a firewall, check that Waterfox has permission to access the Web.";
     // int words_amount = 0;
     // for (int i = 0; i < str.size(); i++)
     // {

     //      if (str[i] == ' ')
     //      {
     //           words_amount++;
     //      }
     //      if ((i) == str.size() - 1)
     //      {
     //           words_amount++;
     //      }
     // };
     // cout << words_amount << " words.";

int ival=4;
bool bval=true;
ival=bval;
cout<< ival;
}